// We are going to have an array of items.
// We are going to create a function with 2 parameters
// The first parameter will be and array and search
// The second parameter will be a value to find

// Inside the function, use a for a loop to go through the array
// And return true if it finds the second parameter inside the array
// Otherwise return false


var items = ["Cat", "Dog", "Snake", "Ferret", "Big Foot"];

var findItem = "Big Foot";

myArray = ["Cat", "Dog", "Snake", "Ferret", "Big Foot"]

function (items, findItem) {
	for( var i = 0; < items.length; i++) {
	if(items[i] == findItem){
		console.log(true);
		}

	} 

} 

find(); 

 
/*

var items = 

var findItem = "Big Foots Wife";

var searchItems = function(haystack, needle) {


